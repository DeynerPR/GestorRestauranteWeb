@model GestorDeRestaurante.Model.Medida

@{
    ViewData["Title"] = "Detalle";
}

<br/>
<h1>Detalle de la Medida</h1>

<br/>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>

<br/>
<div>
    <a asp-action="Index">Volver al catálogo de Medidas</a> |  
    @Html.ActionLink("Editar", "Editar", new { id = Model.Id })
</div>
